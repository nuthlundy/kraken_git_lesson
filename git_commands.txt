# git init
# git add .
# git commit -m "Your process name goes here"
# git status
# git log (hit'q' to exit)
# git checkout (unique commit id)

Git Remote Commands ...
# git remote add origin (url)
# git remote -v
# git push origin master
# git pull origin master